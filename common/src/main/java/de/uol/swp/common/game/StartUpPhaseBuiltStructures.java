package de.uol.swp.common.game;

/**
 * Enum to put the steps of the founding phase in order
 *
 * @author Sven Ahrens
 * @since 2021-05-03
 */
public enum StartUpPhaseBuiltStructures {
    NONE_BUILT,
    FIRST_SETTLEMENT_BUILT,
    FIRST_BOTH_BUILT,
    SECOND_SETTLEMENT_BUILT,
    ALL_BUILT
}